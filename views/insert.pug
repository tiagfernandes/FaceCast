extends layout

block content
    div(id='main')
      div(class='inner')
        header(id='header')
          h1= title

        section(id='banner')
          div(class='content')
            header
              h2= 'Formulaire'
            form(id="addEvent", name="adduser",method="post",action="/insert/add")
              input(id="name", type="text", placeholder="Name", name="name")     
              select(id="type", placeholder="Type", name="type")
                option -- Type --
                option Film
                option Spéctacle
              input(id="date", type="text", placeholder="Date", name="date")
              input(id="nbJours", type="text", placeholder="Nombre jours", name="nbJours")
              
              button#btnSubmit(type="submit") submit